import classNames from "classnames/bind";
import "./Sidebar.module.scss";

// const cx = classNames.bind(styles);

function Sidebar() {
  return (
    <aside>
      <h2 className="test">SIDE BAR</h2>
    </aside>
  );
}

export default Sidebar;
